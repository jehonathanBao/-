# 合约主力监控 · 全新金融指挥台

用户已确认：完整重做 UI，不沿用固定侧栏和旧卡片排列；科技感、金融感、动态效果；产品名称改为“合约主力监控”。

## 界面与信息结构

- 顶部两层导航：品牌、五个工作区、动效开关；第二层展示当前工作区全部目的地。完整模块目录保留所有旧地址与别名，加入原本可访问的强平观察入口，不增加执行能力。
- 总览：全宽市场工作台，左侧紧凑市场入口，中间实际价格样本与订单流读数，右侧最新主力观察；底部独立事件时间线与采集状态。所有值均来自现有快照。
- BTC/ETH 合约：报价带、大幅事件价格散点图、可点击事件时间轴、证据分析栏；筛选和事件表移到全宽区域，深入研究按主题组织。保留原详情、过滤、分页、生命周期、轨迹和数据质量内容。
- 现货、订单流、新币、山寨、强平、信号、历史、规则、Discord、设置、指南统一使用新导航、主题、表头、表格、输入和详情视觉。主标题、浏览器标题、导航品牌及帮助页产品标题统一更名；业务中的有毒订单术语不改变定义。
- 深海蓝画布、电光蓝操作强调、银白等宽数据，金融红绿只表达方向/风险，等级使用原有统一映射。没有装饰性随机行情、随机计数、身份确认或新下单按钮。
- 动效：短暂数据更新高亮、细网格扫描、时间轴激活、展开与切换反馈；只用轻量 CSS/SVG，支持现有总开关、系统 reduced motion 与后台标签停止。
- 桌面 1440/1920、手机 390：导航可横滚、内容可换行，表格在自身容器滚动，弹层保持键盘可达与原确认边界。

## 实现与安全

保留 React 19、Vite 7、Tailwind 3、Heroicons、ECharts；不引入新依赖。布局可以重写，API、Rust、轮询频率、评级、通知逻辑、凭据与生产环境保持不变。原 PRODUCT.md 的侧栏和页面排列规则由本次明确授权覆盖，其风险语义与可访问性要求继续遵守。

先本地实现、测试和隔离预览；不自动提交、推送、部署。用户原有未跟踪文件原样保留。浏览器验收数据只存在于明确标记的隔离测试环境，所有修改/发送请求拦截。

## 验收

顶部导航覆盖原所有路由、别名与当前项，品牌统一；事件时间轴按时间显示且能打开原详情；动效开关与减弱动效有效；空/错误/未启用状态不伪造正常值；现有安全回归、完整前端测试和生产构建通过；关键页面桌面/手机无页面横向溢出。
