# Trellis Tasks

Task directories created by `.trellis/scripts/task.py` live here.

- Active tasks stay at the root of this folder.
- Archived tasks move to `archive/YYYY-MM/`.
- Each task should have a `task.json` and `prd.md` before implementation starts.
