# Product

## Register

product

## Users

个人交易观察者和风控操作者，在桌面或手机浏览器上查看 BTC/ETH 合约、现货以及 Binance 山寨合约异常信号。用户通常处在实盘观察场景，需要快速判断信号方向、严重程度、数据质量和 Discord 告警状态。

## Product Purpose

这个产品是只读的盘口和成交流异常监控台。它聚合合约、现货、TOF、有毒订单和主力行为候选信号，帮助用户识别短线风险、主力合约异动、现货确认和极端行情冲击。成功标准是信号解释清楚、误报被克制、页面刷新不丢信号，并且不会触发下单、撤单、资金或账户操作。

## Brand Personality

冷静、专业、克制。界面应像一块夜间使用的交易雷达屏，优先服务快速扫描、风险确认和安全边界，而不是做营销式视觉效果。

## Anti-references

不要做成营销落地页、AI 紫色渐变大屏、装饰性玻璃卡片、炫技动画或难以扫描的巨大信息墙。不要把 Medium/Low 展示成需要外部告警的紧急事件。不要让 disabled、spot_only、dry-run、未配置 webhook 等状态看起来像故障。

## Design Principles

1. 先解释风险，再展示技术细节。
2. 高风险信号要醒目，但非高风险状态要克制。
3. 数据源状态必须明确区分在线、降级、未启用、仅现货和 dry-run。
4. 所有告警和按钮都保持只读语义，不暗示会自动交易。
5. 桌面优先做密集扫描，移动端优先做可读卡片和横向滚动降级。

## Accessibility & Inclusion

默认按 WCAG AA 对比度处理，减少低对比灰字。状态不能只依赖颜色，需有文字标签。动效仅用于状态反馈，尊重 reduced motion。表格和详情入口需要键盘可达，错误、空状态和加载状态要有清晰文案。

## Frontend Baseline

核心页面层级采用“状态条 -> 60s flow -> 数据质量 / 平台口径 -> 筛选 -> 信号表格 -> 详情弹窗”。首页、CWM、BACM 都应避免巨大平台说明墙，平台能力和运行状态优先压缩成顶部小状态块或紧凑条。

视觉 token 以深色交易台为基准：

- 主面板使用 `console-panel`，圆角保持 `rounded-xl`，阴影克制。
- 次级卡片使用 `console-panel-muted`，只承载状态、说明和轻量指标。
- 表单使用 `console-field`，聚焦时显示 cyan ring。
- 表格行使用 `console-row`，支持 hover 和键盘 focus。
- 小标题使用 `console-label`，大写间距保持克制，不用于正文。

交互基线：

- 侧栏所有主要模块始终可见，active 状态必须有文字、背景和 ring，而不是只靠颜色。
- 筛选控件必须有 label，可键盘聚焦，移动端保持网格换行。
- 表格在窄屏允许横向滚动，不压缩关键数字到不可读。
- 信号详情弹窗最大高度限制在视口内，并允许纵向滚动。
- 所有高风险 / dry-run / disabled / spot_only / 未配置 webhook 状态必须以文字解释，不把只读提醒误表现为交易执行能力。
